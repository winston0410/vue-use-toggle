{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "type-check": "tsc"
  },
  "dependencies": {
    "vue": "^3.0.11",
    "vue-use-toggle": "git+https://github.com/winston0410/vue-use-toggle.git"
  },
  "devDependencies": {
    "@snowpack/plugin-dotenv": "^2.1.0",
    "@snowpack/plugin-vue": "^2.4.0",
    "snowpack": "^3.3.7",
    "typescript": "^4.2.4"
  }
}
